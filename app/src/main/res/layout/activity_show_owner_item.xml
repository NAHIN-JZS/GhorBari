<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    tools:context=".ShowOwnerItem">


    <ProgressBar
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/progressbarShowOwnerId"
        android:paddingLeft="1000dp"
        android:secondaryProgress="@android:integer/config_mediumAnimTime"
        android:visibility="gone"
        />

    <ListView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/showOwnerItemList"
        android:padding="10dp"
        android:divider="@android:color/holo_blue_light"
        android:dividerHeight="3dp"
        android:listSelector="#E9CDDC39"/>


</LinearLayout>