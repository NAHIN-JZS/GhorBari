<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ghorbari2">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AboutApp"></activity>
        <activity android:name=".AboutMe" />
        <activity
            android:name=".nev_all"
            android:label="@string/title_activity_nev_all"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ShowOwnerItem" />
        <activity android:name=".ShowAllOwnerRoom" />
        <activity android:name=".AddHouse" />
        <activity android:name=".ownerProfile" />
        <activity android:name=".showItemlist" />
        <activity android:name=".renterProfile" />
        <activity android:name=".signUp" />
        <activity android:name=".SignIn" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>